var colors = [
	"red",
	"orange",
	"yellow",
	"green",
	"indigo",
	"violet"
	];

var count = 0;

var cycle = function(array) {
	console.log(array[count]);
	if (count == array.length-1) {
		count = 0;
	}else{

	};
};

var display = function(){
	cycle(colors);
};